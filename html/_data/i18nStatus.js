// Locales listed here must have an exact page-for-page counterpart for every
// canonical English route. Keep a locale out while its translation is being
// prepared; add it only in the commit that completes the full route set.

module.exports = {
    completeLocales: [],
};
